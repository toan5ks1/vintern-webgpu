{
  "name": "vintern-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm --filter @huggingface/transformers build && pnpm --filter vintern-app build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "pnpm": "^9.0.0",
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@9.1.0"
}
